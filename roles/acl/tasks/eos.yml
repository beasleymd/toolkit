---
- name: Parse ACL
  arista.eos.eos_acls:
    state: parsed
    running_config: "{{ acl_config }}" #Found in host_vars/{{device}}.yml
  register: eos_acl
 
- name: Configure ACL
  arista.eos.eos_acls:
    state: merged
    config: "{{ eos_acl.parsed }}" 
